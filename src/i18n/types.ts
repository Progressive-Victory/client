import { FluentVariable } from "@fluent/bundle";

export type fluentVariables = Record<string, FluentVariable>;

export { Locale, LocalizationMap } from 'discord.js';

export const common = 'common';
